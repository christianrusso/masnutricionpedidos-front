// This file can be replaced during build by using the `fileReplacements` array.
// `ng build` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  apiCanal: 'http://localhost:3000/api/canal',
  apiCliente: 'http://localhost:3000/api/cliente',
  apiCondiciones: 'http://localhost:3000/api/condiciones',
  apiFamilia: 'http://localhost:3000/api/familia',
  apiIVA: 'http://localhost:3000/api/iva',
  apiPermiso: 'http://localhost:3000/api/permiso',
  apiProducto: 'http://localhost:3000/api/producto',
  apiRegla: 'http://localhost:3000/api/reglaComercial',
  apiTelefono: 'http://localhost:3000/api/telefono',
  apiUsuario: 'http://localhost:3000/api/usuario',
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/plugins/zone-error';  // Included with Angular CLI.
